<!DOCTYPE html>
<html>
<head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<title>Page Title</title>
</head>
<body>

<div style="height: 50px"></div>


<div class="text-center text-white">
    <h1 class="display-3 very-bold bit-of-shadow">Shot on Sony A6400, Sigma 30mm f/1.4</h1>
</div>
<% partitioned_list = @all_shoots.to_a.in_groups(3, false).to_a %>

<div class="container-fluid">
    <div class="row">
        <div class="column">
            <% if (partitioned_list.length() > 0) %>
                <% partitioned_list[0].each do |shoot| %>
                    <%= image_tag shoot.cover_image%>
                <% end %>
            <% end %>
        </div>

        <div class="column">
            <% if (partitioned_list.length() > 1) %>
                <% partitioned_list[1].each do |shoot| %>
                    <%= image_tag shoot.cover_image%>
                <% end %>
            <% end %>
        </div>

        <div class="column">
            <% if (partitioned_list.length() > 2) %>
                <% partitioned_list[2].each do |shoot| %>
                    <%= image_tag shoot.cover_image%>
                <% end %>
            <% end %>
        </div>

    </div>

</div>


</body>
</html>